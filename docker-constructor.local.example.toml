# Example local companion to docker-constructor.toml.
#
# This file is tracked for reference only — your actual local companion
# (docker-constructor.local.toml) is ignored by git and never committed.
#
# Fields written by `./docker/docker-constructor.py doctor`:
#
#   [host-access]
#   address = "10.0.2.2"   # concrete gateway IP (docker-gateway mode)
#
# Fields you may add manually:
#
#   [cache]
#   # Absolute, dedicated constructor-owned root. Do not use /, $HOME,
#   # XDG_CACHE_HOME itself, or an ancestor of XDG_CACHE_HOME.
#   dir = "/path/to/docker-constructor-custom"
#
#   [corporate-trust]
#   enabled = true  # requires .docker-local/corporate-ca-bundle.crt
#
#   [network.proxy]
#   url = "http://proxy.corp.example:3128"  # also: socks5, socks5h
#   no_proxy = "localhost,.corp.example"    # optional; explicit only
#
#   [output]
#   # interactive (default), lines (explicit noninteractive output), or off
#   host_heartbeat = "interactive"
#   # Privacy default: normalized network hostnames remain hidden.
#   show_network_hosts = false

[host-access]
address = "10.0.2.2"

[cache]
dir = "/home/dev/.cache/docker-constructor-custom"

[output]
host_heartbeat = "interactive"
show_network_hosts = false
